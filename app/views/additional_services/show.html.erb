<div class="container mt-4">
  <div class="card rounded-4 shadow">
    <div class="card-header bg-primary text-white rounded-top-4">
      <h2>Detalhes do Serviço Adicional</h2>
    </div>

    <div class="card-body">
      <h5 class="card-title"><%= @service.name %></h5>
      <p class="card-text"><strong>Valor:</strong> <%= @service.value %></p>
    </div>

    <div class="card-footer text-end">
      <%= link_to 'Voltar', additional_services_path, class: 'btn btn-outline-secondary' %>
      <%= link_to 'Editar', edit_additional_service_path(@service), class: 'btn btn-primary' %>
    </div>
  </div>
</div>
